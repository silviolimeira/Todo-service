import { ObserverWithSubscription } from './observer-with-subscription';

describe('ObserverWithSubscription', () => {
  it('should create an instance', () => {
    expect(new ObserverWithSubscription()).toBeTruthy();
  });
});
